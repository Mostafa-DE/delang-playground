export const testCode = `logs("Hello World!")

const logName = fun() {
  return "DE Lang! 🚀";
}

const num1 = 15;
let num2 = 10;

if num1 > num2: {
  logs(logName());
} else {
  logs("Nothing to show :(");
}
`;
